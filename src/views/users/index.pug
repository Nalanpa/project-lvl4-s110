extends /layouts/application.pug

block content
  table.table.table-striped.table-hover.table-sm.mt-5
    thead.thead-inverse
      tr
        th Full Name
        th Email
        th Created At
    tbody
      each user in users
        tr
          td= user.fullName
          td= user.email
          td= user.createdAt
