extends /layouts/application.pug


block content
  
  .row.justify-content-center
    .col-lg-6.col-12.p-4
 
        table.table.table-striped.table-hover.table-sm.mt-4
          thead.thead-inverse
            tr
              th Tags
              th 
          tbody
            each tag in addedTags  
              tr
                td= tag.name
                td
                  a(href=`${urlFor('tagsTasks', { id: tag.id })}`) Tasks
            each tag in freeTags  
              tr
                td= tag.name
                td
                  a.text-danger(data-confirm="Are you sure you want to delete this tag?" href=urlFor('tagsDelete', tag.id) data-method='delete' rel='nofollow') Delete  

        
